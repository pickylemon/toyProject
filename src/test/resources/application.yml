spring:
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:D:/Users/lemon/2024TOYPROJECT/h2TestDB/testdb
    username: sa
    password:
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: create-drop
    show-sql: true
  h2:
    console:
      enabled: true
  application:
    name: toyProject
